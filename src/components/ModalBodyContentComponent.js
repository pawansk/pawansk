import React from "react";

const ModalBodyContentComponent = ({children}) =>{
    return(
        <div>  {children}  </div>/* Render the only Modal Body Content Area */

    );
};
 export default ModalBodyContentComponent;
