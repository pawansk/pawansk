import PageTitle from "../components/PageTitle";

const BulkReport = () =>{
const PageHeading = "Bulk Uploading Reports";

    return(
        <div> 
             <PageTitle PageHeading={PageHeading}></PageTitle>
        </div>
    );
};

export default BulkReport;